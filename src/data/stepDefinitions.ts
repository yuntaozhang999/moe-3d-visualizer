import { ForwardStep } from '../types/model';
import { EQUATION_DEFINITIONS } from './equationData';

const RAW_FORWARD_STEPS: Omit<ForwardStep, 'formulaData'>[] = [
  {
    id: 'input_tokens',
    order: 1,
    name: '1. Input Tokens & token_ids',
    category: 'Input',
    shortDesc: 'Discrete text prompt tokenized into integer IDs.',
    longDesc: 'The input prompt sequence is tokenized using the 128,256-vocabulary Marin tokenizer into an integer sequence of token IDs.',
    mathFormula: 'x_{\\text{tokens}} = [t_1, t_2, \\dots, t_S], \\quad t_i \\in \\{0, 1, \\dots, V-1\\}',
    codeSnippet: 'token_ids: Int[Array, "B S"]  # B=1, S=4096 (toy: 6)',
    realShape: '[1, 4096]',
    visualShape: '[1, 6]',
    hardwareSignificance: 'Marin tokenizer uses an expanded 128k vocabulary for optimal multilingual & code tokenization compression.',
    cameraFocus: [-16.5, 2, 0],
    cameraPos: [-16.5, 5, 8],
    activeNodeIds: ['node_tokens'],
  },
  {
    id: 'token_embed',
    order: 2,
    name: '2. Embedding Gather (_embedding_gather)',
    category: 'Input',
    shortDesc: 'Untied embedding table (788M params learned during pre-training) mapping token IDs to 6144-dim vectors.',
    longDesc: 'Embedding matrix maps each discrete token ID to a dense vector in the 6144-dimensional model latent space. Untied from LM output head.',
    mathFormula: 'E = \\text{Gather}(W_{\\text{embed}}, t), \\quad W_{\\text{embed}} \\in \\mathbb{R}^{128256 \\times 6144}',
    codeSnippet: 'hidden = _embedding_gather(self.token_embed, token_ids)',
    realShape: '[1, 4096, 6144]',
    visualShape: '[1, 6, 64]',
    hardwareSignificance: '788M dense parameters trained end-to-end during pre-training. At inference, fully replicated across data shards for zero-communication local gather.',
    cameraFocus: [-13.8, 2.0, -0.8],
    cameraPos: [-13.8, 6.5, 9.5],
    activeNodeIds: ['node_w_embed', 'op_embed_gather', 'node_embed'],
  },
  {
    id: 'embed_gated_norm',
    order: 3,
    name: '3. Embedding GatedNorm',
    category: 'Residual & Norm',
    shortDesc: 'RMSNorm wrapped with rank-128 GatedNorm bottleneck.',
    longDesc: 'Applies RMSNorm followed by a rank-128 bottleneck GatedNorm to control activation magnitude and suppress early training gradient spikes.',
    mathFormula: 'y = \\text{RMSNorm}(x) \\odot \\sigma(\\text{SiLU}(\\text{RMSNorm}(x) W_{\\text{down}}) W_{\\text{up}})',
    codeSnippet: 'hidden = self.embed_gated_norm(self.embed_norm(hidden))',
    realShape: '[1, 4096, 6144] (Rank: 128)',
    visualShape: '[1, 6, 64]',
    hardwareSignificance: 'Compensates for AdamH/MuonH bounded activation norm drift without sacrificing capacity.',
    cameraFocus: [-8.8, 2, 0],
    cameraPos: [-8.8, 5, 8],
    activeNodeIds: ['node_embed_norm', 'op_embed_gn'],
  },
  {
    id: 'pre_attn_gated_norm',
    order: 4,
    name: '4. Pre-Attention GatedNorm',
    category: 'Attention',
    shortDesc: 'Block input normalisation before Attention branch.',
    longDesc: 'Standard pre-norm block: residual stream is normalized with RMSNorm and modulated with a layer-specific rank-128 GatedNorm.',
    mathFormula: 'x_{\\text{attn\\_in}} = \\text{GatedNorm}_{\\text{attn}}(\\text{RMSNorm}(x))',
    codeSnippet: 'attn_in = self.attn_gated_norm(self.rms_attn(x))',
    realShape: '[1, 4096, 6144]',
    visualShape: '[1, 6, 64]',
    hardwareSignificance: 'Prevents inter-layer residual stream blowup in deep 48-layer stack.',
    cameraFocus: [-6.8, 2, 0],
    cameraPos: [-6.8, 5, 8],
    activeNodeIds: ['node_pre_attn_norm', 'op_attn_gn'],
  },
  {
    id: 'qkv_proj',
    order: 5,
    name: '5. QKV Projections (GQA)',
    category: 'Attention',
    shortDesc: 'Linear projections into Query, Key, and Value spaces.',
    longDesc: 'Projects 6144-dim hidden states into 48 Q heads (dim 128) and GQA KV heads (12 for Local layers, 6 for Global layers).',
    mathFormula: 'Q = x W_Q, \\; K = x W_K, \\; V = x W_V',
    codeSnippet: 'q_flat = jnp.einsum("bsh,hd->bsd", x, self.w_q)\nk_flat = jnp.einsum("bsh,hd->bsd", x, self.w_k)',
    realShape: 'Q: [48, 128], K/V: [12/6, 128]',
    visualShape: 'Q: [12, 16], K/V: [3, 16]',
    hardwareSignificance: 'GQA 4:1 (local) and 8:1 (global) slashes KV cache memory by 75%–87.5% during long-context inference.',
    cameraFocus: [-1.6, 2.5, -2],
    cameraPos: [-1.6, 7, 9],
    activeNodeIds: ['node_q', 'node_k', 'node_v'],
  },
  {
    id: 'short_conv_k',
    order: 6,
    name: '6. ShortConv on Key (K-channel)',
    category: 'Attention',
    shortDesc: 'Kernel=4 causal depthwise 1D conv on Key projections.',
    longDesc: 'A depthwise causal 1D convolution over sequence axis mixes each key channel with its 3 causal predecessors: sum_{lag=0..3} w[lag] * k[t-lag].',
    mathFormula: 'K_{\\text{conv}}[t] = \\sum_{\\tau=0}^{3} w_{\\tau} \\odot K[t - \\tau], \\quad w_0=1, w_{1..3}=0 \\; (init)',
    codeSnippet: 'if self.sconv_k is not None:\n    k_flat = self.sconv_k(k_flat, sconv_segment_ids)',
    realShape: 'Kernel: [4, 1536/768]',
    visualShape: '[4, 48]',
    hardwareSignificance: 'Shard-local depthwise conv: zero communication overhead across GPU nodes while boosting local context awareness.',
    cameraFocus: [0.2, 2, -2],
    cameraPos: [0.2, 5, 7],
    activeNodeIds: ['node_k', 'op_sconv_k'],
  },
  {
    id: 'q_k_norm_rope',
    order: 7,
    name: '7. Q/K RMSNorm & Half-RoPE',
    category: 'Attention',
    shortDesc: 'Per-head Q/K RMSNorm + Local Half-RoPE or Global NoPE.',
    longDesc: 'Applies per-head RMSNorm to Q and K. On Local layers, Rotary Position Embedding (RoPE) is applied ONLY to first half (64 dims) of head_dim. Global layers disable RoPE (NoPE).',
    mathFormula: 'Q = \\text{RMSNorm}(Q) \\cdot 1.3, \\; Q_{[:64]} = \\text{RoPE}(Q_{[:64]}), \\; Q_{[64:]} = Q_{[64:]}',
    codeSnippet: 'q = rms_norm(q) * self.cfg.qk_mult\n# Half-RoPE on local, NoPE on global\nq, k = _apply_rotary_embedding_fused(q, k, rotary_dim=64, disable_rope=is_global)',
    realShape: 'Head dim: 128 (RoPE on 64, NoPE on 64)',
    visualShape: 'Head dim: 16 (RoPE on 8, NoPE on 8)',
    hardwareSignificance: 'qk_mult (1.3) improves softmax temperature stability; Half-RoPE prevents high-frequency positional rot degradation.',
    cameraFocus: [2.1, 3.4, -2],
    cameraPos: [2.1, 6, 7],
    activeNodeIds: ['node_q', 'node_k', 'op_rope'],
  },
  {
    id: 'attention_weights',
    order: 8,
    name: '8. Attention Softmax & Windowing',
    category: 'Attention',
    shortDesc: 'Local 2048 Sliding Window vs Global Full Causal Attention.',
    longDesc: 'Computes Q @ K^T / sqrt(d). Local layers apply a strict 2048-token sliding window causal band; Global layers compute full causal attention.',
    mathFormula: 'A_{ij} = \\text{Softmax}\\left( \\frac{Q_i K_j^\\top}{\\sqrt{d_k}} + M_{ij} \\right), \\quad M_{ij} = \\begin{cases} 0 & 0 \\le i-j \\le W \\\\ -\\infty & \\text{otherwise} \\end{cases}',
    codeSnippet: 'attn_out = attention(q, k, v, mask, implementation="gpu_fa4_cute_wide")',
    realShape: '[4096, 4096] (Sparsified 2048)',
    visualShape: '[6, 6] Attention Heatmap',
    hardwareSignificance: 'Sliding window drastically reduces O(S^2) attention FLOPs on 3 out of 4 layers, keeping memory bandwidth high.',
    cameraFocus: [5.5, 2.2, -1.8],
    cameraPos: [5.5, 6, 8],
    activeNodeIds: ['node_attn_matrix'],
  },
  {
    id: 'xsa_decorrelation',
    order: 9,
    name: '9. XSA (Exclusive Self-Attention)',
    category: 'Attention',
    shortDesc: 'Decorrelation projection subtracting the V-parallel component.',
    longDesc: 'XSA forces self-attention to learn novel features rather than merely copying value states by orthogonalizing the output against V per head.',
    mathFormula: 'z_i = y_i - \\left( \\frac{y_i^\\top v_i}{\\|v_i\\|^2 + 10^{-6}} \\right) v_i',
    codeSnippet: 'dot = jnp.sum(attn_out * aligned_v, axis=-1, keepdims=True)\nv_norm_sq = jnp.sum(aligned_v * aligned_v, axis=-1, keepdims=True)\nattn_out = attn_out - (dot / (v_norm_sq + 1e-6)) * aligned_v',
    realShape: '[48 heads, 128 dim]',
    visualShape: '[12 heads, 16 dim]',
    hardwareSignificance: 'Ablations show XSA improves downstream reasoning and in-context sample efficiency by eliminating redundant V-copying pathways.',
    cameraFocus: [8.3, 2.2, -1.4],
    cameraPos: [8.3, 5, 7],
    activeNodeIds: ['op_xsa', 'node_attn_out'],
  },
  {
    id: 'head_gating',
    order: 10,
    name: '10. Attention Head Gate',
    category: 'Attention',
    shortDesc: 'Learned scalar per-head gating modulated by residual token.',
    longDesc: 'A linear projection computes a dynamic gate scalar per head: gate = 2 * sigmoid(x @ W_gate). Dynamically suppresses unneeded heads.',
    mathFormula: 'g_h = 2 \\cdot \\sigma(x \\cdot W_{\\text{gate}}^{(h)}), \\quad \\text{attn\\_out}_h = g_h \\cdot z_h',
    codeSnippet: 'gate = 2 * jax.nn.sigmoid(jnp.einsum("bsd,dn->bsn", x, self.attn_gate))[..., None]\nattn_out = gate * attn_out',
    realShape: '[1, 4096, 48 heads]',
    visualShape: '[1, 6, 12 heads]',
    hardwareSignificance: 'Acts as dynamic head pruning/amplification without the latency of MoE routing.',
    cameraFocus: [10.4, 2.2, -0.8],
    cameraPos: [10.4, 5, 7],
    activeNodeIds: ['op_head_gate', 'node_attn_out'],
  },
  {
    id: 'attn_proj_residual',
    order: 11,
    name: '11. W_O Projection & Residual Add',
    category: 'Residual & Norm',
    shortDesc: 'Concatenate heads, project to 6144, add to residual stream.',
    longDesc: 'Projects concatenated head outputs back to hidden_dim=6144 through W_O, and adds directly back into the bypass residual stream.',
    mathFormula: 'x = x + \\text{attn\\_out} \\cdot W_O',
    codeSnippet: 'x = x + jnp.einsum("bsh,hd->bsd", attn_out, self.w_o)',
    realShape: '[1, 4096, 6144]',
    visualShape: '[1, 6, 64]',
    hardwareSignificance: 'Model-axis sharding on W_O allows all-reduce/reduce-scatter to overlap with downstream LatentMoE compute.',
    cameraFocus: [13.5, 2, 0],
    cameraPos: [13.5, 6, 9],
    activeNodeIds: ['node_attn_res', 'op_attn_add'],
  },
  {
    id: 'pre_moe_gated_norm',
    order: 12,
    name: '12. Pre-MoE GatedNorm',
    category: 'MoE',
    shortDesc: 'Pre-normalization before LatentMoE dispatch.',
    longDesc: 'Applies RMSNorm and rank-128 GatedNorm before sending tokens to both the router, latent down-projection, and concurrent shared experts.',
    mathFormula: 'x_{\\text{mlp\\_in}} = \\text{GatedNorm}_{\\text{mlp}}(\\text{RMSNorm}(x))',
    codeSnippet: 'mlp_in = self.mlp_gated_norm(self.rms_mlp(x))',
    realShape: '[1, 4096, 6144]',
    visualShape: '[1, 6, 64]',
    hardwareSignificance: 'Ensures the router and experts receive properly scaled inputs regardless of residual accumulation.',
    cameraFocus: [16.1, 2, 0],
    cameraPos: [16.1, 5, 8],
    activeNodeIds: ['node_pre_moe_norm', 'op_moe_gn'],
  },
  {
    id: 'router_qb_selection',
    order: 13,
    name: '13. LatentMoE Router & QB Selection',
    category: 'MoE',
    shortDesc: 'Router selects Top-8 of 384 experts via QB routing.',
    longDesc: 'Reads full 6144-dim token. Computes logits over 384 active half-width experts. Selects top-8 + threshold alpha. Sigmoid weights renormed to sum to 2.5.',
    mathFormula: 's = x W_r, \\; \\text{top8} = \\text{top\\_k}(s + b, 8), \\; w_i = \\sigma(s_i) \\cdot \\frac{2.5}{\\sum_{j \\in \\text{top8}} \\sigma(s_j)}',
    codeSnippet: '_topk_logits, selected_experts = jax.lax.top_k(biased_logits, 8 + 1)\ncombine_weights = sigmoid(unbiased_topk) * (2.5 / (sum + 1e-9))',
    realShape: 'Logits: [4096, 384], Selected: [4096, 8]',
    visualShape: 'Logits: [6, 24], Selected: [6, 8]',
    hardwareSignificance: 'QB (Quantile Balancing) dynamically balances load across GPU nodes without intrusive auxiliary balance loss penalties.',
    cameraFocus: [19.2, 2.2, 3.5],
    cameraPos: [19.2, 6, 11],
    activeNodeIds: ['node_router', 'op_router_qb'],
  },
  {
    id: 'latent_compression',
    order: 14,
    name: '14. Latent Down-Proj (6144 -> 3072)',
    category: 'MoE',
    shortDesc: 'Token compression to latent_dim + learnable latent RMSNorm.',
    longDesc: 'Crucial Marin architectural feature: tokens are compressed from hidden_dim (6144) to latent_dim (3072) and normalized by latent RMSNorm before expert transport.',
    mathFormula: 'x_{\\text{latent}} = \\text{RMSNorm}_{\\text{latent}}(x \\cdot W_{\\text{latent\\_down}}), \\quad W_{\\text{latent\\_down}} \\in \\mathbb{R}^{6144 \\times 3072}',
    codeSnippet: 'routed_input = jnp.einsum("td,dl->tl", x_flat, self.w_latent_down)\nrouted_input = self.latent_norm(routed_input)',
    realShape: '[1, 4096, 3072]',
    visualShape: '[1, 6, 32]',
    hardwareSignificance: 'Cuts EP all-to-all cross-node network bandwidth in HALF (150 GB/s -> 75 GB/s), preventing network bottlenecks on 11-rack clusters.',
    cameraFocus: [19.8, 0, -1],
    cameraPos: [19.8, 4, 7],
    activeNodeIds: ['node_latent_down', 'op_latent_norm'],
  },
  {
    id: 'routed_experts_swiglu',
    order: 15,
    name: '15. 8 Routed Half-Width Experts (SwiGLU)',
    category: 'MoE',
    shortDesc: 'Selected 8 experts process 3072-dim tokens in parallel.',
    longDesc: 'Each of the 8 selected experts has hidden 3072 and intermediate 3072 (half-width). Employs SwiGLU activation: (x W_gate * silu(x W_up)) W_down.',
    mathFormula: '\\text{Expert}_k(x) = (\\text{SiLU}(x W_{\\text{gate}}^{(k)}) \\odot x W_{\\text{up}}^{(k)}) W_{\\text{down}}^{(k)}',
    codeSnippet: 'moe_out = self.expert_mlp(routed_input, selected_experts, combine_weights)',
    realShape: '8 experts x [3072, 3072]',
    visualShape: '8 active tiles x [16, 16]',
    hardwareSignificance: 'Half-width experts (hidden/2) maximize parameter diversity across the 384-expert pool while keeping memory within HBM budgets.',
    cameraFocus: [28.2, 1.0, -1.2],
    cameraPos: [28.2, 5, 8],
    activeNodeIds: ['node_experts_routed'],
  },
  {
    id: 'shared_experts_swiglu',
    order: 16,
    name: '16. 2 Concurrent Shared Experts (SwiGLU)',
    category: 'MoE',
    shortDesc: '2 always-active shared experts processing full 6144-dim token.',
    longDesc: 'Operates concurrently with routed experts! 2 shared experts compute on full-width token (hidden 6144 -> intermediate 3072 -> hidden 6144) to capture common knowledge.',
    mathFormula: '\\text{Shared}(x) = \\sum_{s=1}^{2} \\text{SwiGLU}_s(x_{\\text{mlp\\_in}})',
    codeSnippet: 'for shared_expert in self.shared:\n    mlp_out = mlp_out + shared_expert(mlp_in, activation=silu)',
    realShape: '2 x [6144 -> 3072 -> 6144]',
    visualShape: '2 x [64 -> 32 -> 64]',
    hardwareSignificance: 'Ensures fundamental syntactic & reasoning patterns are shared across all tokens without occupying routing capacity.',
    cameraFocus: [27.0, 5.2, 2.5],
    cameraPos: [27.0, 9, 10],
    activeNodeIds: ['node_experts_shared'],
  },
  {
    id: 'moe_aggregation_residual',
    order: 17,
    name: '17. Latent Up-Proj & Residual Merge',
    category: 'Residual & Norm',
    shortDesc: 'Up-project routed (3072 -> 6144), add Shared & Residual.',
    longDesc: 'Combines weighted routed expert outputs, up-projects from 3072 back to 6144 via W_latent_up, adds the 2 Shared Experts outputs, and adds back into residual stream.',
    mathFormula: 'x = x + \\left( (\\sum_{k=1}^8 w_k \\text{Expert}_k) W_{\\text{latent\\_up}} + \\text{Shared}_1 + \\text{Shared}_2 \\right)',
    codeSnippet: 'routed_flat = jnp.einsum("tl,ld->td", routed_flat, self.w_latent_up)\nmlp_out = mlp_out + routed_out\nx = x + mlp_out',
    realShape: '[1, 4096, 6144]',
    visualShape: '[1, 6, 64]',
    hardwareSignificance: 'Completes the dual-path MoE computation for this block. Both paths fuse cleanly into the residual highway.',
    cameraFocus: [35.0, 2, 0],
    cameraPos: [35.0, 6, 9],
    activeNodeIds: ['node_moe_res', 'op_moe_add'],
  },
  {
    id: 'final_gated_norm',
    order: 18,
    name: '18. Final Layer GatedNorm',
    category: 'Residual & Norm',
    shortDesc: 'Final normalization after 48 Transformer layers.',
    longDesc: 'After traversing all 48 layers (or at the output of the current block), the representation passes through final RMSNorm and rank-128 GatedNorm.',
    mathFormula: 'x_{\\text{final}} = \\text{GatedNorm}_{\\text{final}}(\\text{RMSNorm}(x))',
    codeSnippet: 'hidden = self.final_gated_norm(self.final_norm(hidden))',
    realShape: '[1, 4096, 6144]',
    visualShape: '[1, 6, 64]',
    hardwareSignificance: 'Guarantees unit variance and regulated dynamics before projecting into the high-cardinality 128k vocabulary logits.',
    cameraFocus: [37.5, 2, 0],
    cameraPos: [37.5, 5, 8],
    activeNodeIds: ['node_final_norm', 'op_final_gn'],
  },
  {
    id: 'untied_lm_head',
    order: 19,
    name: '19. Untied LM Head & Next Token Logits',
    category: 'Output',
    shortDesc: 'Linear projection to 128,256 vocabulary logits & Softmax.',
    longDesc: 'Independent output projection matrix (untied from embeddings) maps 6144 features to 128,256 logits. Argmax / temperature sampling produces next token.',
    mathFormula: '\\text{logits} = x_{\\text{final}} \\cdot W_{\\text{out}}, \\quad P(w_t) = \\text{Softmax}(\\text{logits}), \\quad W_{\\text{out}} \\in \\mathbb{R}^{6144 \\times 128256}',
    codeSnippet: 'return jnp.einsum("bsh,hd->bsd", hidden, self.output_proj)',
    realShape: '[1, 4096, 128256]',
    visualShape: '[1, 6, 128] Top Predicted Tokens',
    hardwareSignificance: 'Untied LM head gives additional parameter freedom at output, critical for high-perplexity multi-domain 18T hero runs.',
    cameraFocus: [42.4, 2, 0.8],
    cameraPos: [42.4, 6, 9],
    activeNodeIds: ['node_lm_head'],
  }
];

export const FORWARD_STEPS: ForwardStep[] = RAW_FORWARD_STEPS.map((step) => ({
  ...step,
  formulaData: EQUATION_DEFINITIONS[step.id],
}));
